# Default presentation mode. The UI returns realistic mock results after 1.5 seconds.
DETECTION_MODE=mock

# Backend mode. Start the FastAPI app on port 8000, then switch DETECTION_MODE.
# DETECTION_MODE=backend
FASTAPI_DETECT_URL=http://127.0.0.1:8000/detect
